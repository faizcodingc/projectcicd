name: myGreeting
on: push
jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: "greet user"
              run: |
               echo "Hello World!"
               echo "Today's date is: $(date)"
